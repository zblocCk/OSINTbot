[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "osintbot"
version = "0.1.0"
description = "A modular OSINT investigation framework"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    {name = "zblocCk"}
]

[tool.setuptools.packages.find]
where = ["src"]
